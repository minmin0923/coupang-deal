# 필터링 규칙 명세 v2 (확정)

v1에서 바뀐 것: ① 스마트스토어 기준가 → 교차검증용으로 강등 ② 10% 단일컷 → 수집/발송 2단 분리 ③ 배송비 보정 추가 ④ 괴리율 중간구간 배점 상향

---

## STEP 1 — 하드컷 (네이버 호출 전, 비용 절약)

| 조건 | 값 |
|---|---|
| 최저 가격 | 3,000원 |
| 상품명 최소 길이 | 8자 |
| 금지어 | 중고·리퍼·전시·반품·B급·흠집·미개봉·렌탈·정기배송·구독·체험·샘플·1개월·카드할인·쿠폰적용·제휴·예약판매·해외구매 |
| 재발송 금지 | 동일상품 24시간 / 동일상품+동일가격 7일 |

## STEP 2 — 기준가 산출

1. 상품명 → 검색어 변환 (`extract_keyword`)
   - 대괄호·소괄호 제거, 첫 쉼표 앞까지, 마케팅 잡음어 제거, 앞 7단어
   - **⚠ 용량·묶음수량은 전체 문자열에서 따로 추출해 되붙임**
     (`곰곰 삼다수 2L, 24개` → `곰곰 매일 아침 삼다수 2L 24개`)
     이걸 놓치면 기준가가 24배 어긋남 — 가장 흔한 버그
2. 네이버 쇼핑 검색 (`sort=asc`, 20건)
3. 제외: `productType` 4~12 (중고/단종/판매예정), `mallName`에 쿠팡
4. **카탈로그(type 1)** 를 우선 풀로 사용. 3건 미만이면 스토어(2,3) 합산
5. 폐기 조건
   - 유효 표본 3건 미만
   - 최고/최저 `lprice` 비율 10배 이상 (옵션가 착시)
   - 카탈로그 중앙값 vs 스토어 중앙값 3배 이상 괴리
6. `P_ref` = 채택 풀의 **중앙값**

## STEP 3 — 실효가 보정

```
쿠팡 실효가 = productPrice + (로켓 or 무료배송 ? 0 : 3,000)
네이버 실효가 = lprice중앙값 × 1.05        # 배송비 미포함분
괴리율 = 쿠팡실효가 / 네이버실효가
```

## STEP 4 — 점수 (100점)

| 항목 | 배점 | 기준 |
|---|---|---|
| A 괴리율 | 40 | ≤.15→40 / ≤.30→35 / ≤.45→29 / ≤.60→24 / ≤.70→17 / ≤.80→7 / 초과→0 |
| B 자릿수오류 | 20 | `쿠팡가×10` 또는 `×100`이 기준가의 80~120% |
| C 인기도(rank) | 15 | 1~10→15 / ~30→11 / ~60→7 / ~100→3 |
| D 절약액 | 10 | ≥10만→10 / ≥5만→8 / ≥2만→5 / ≥1.5만→2 |
| E 로켓 | 8 | isRocket |
| F 골드박스 | 7 | 골드박스 동시노출 |
| 감점 | | 수량표기 없음 −8 / 저가+배송비부담 −5 |

## STEP 5 — 등급

**발송 게이트**: 괴리율 ≤ 0.70 **AND** 절약액 ≥ 15,000원

| 등급 | 조건 | 처리 |
|---|---|---|
| S | 85점↑ **AND** 자릿수오류 | 즉시 발송, "가격오류 의심" 표기 |
| A | 게이트 통과 + 70점↑ | 정규 발송 상단 |
| B | 게이트 통과 + 50점↑ | 정규 발송 |
| C | 35점↑ | 로그만 |
| D | 그 외 | 폐기 |

안전장치: S등급이 하루 3건 초과 → 로직 오류로 간주하고 **발송 중단 + 경고**

## 검증된 판정 예시

| 케이스 | 괴리율 | 점수 | 등급 |
|---|---|---|---|
| 로켓 상시가 (17% 저렴) | 0.83 | 23 | D |
| 기저귀 43% 할인 | 0.57 | 52 | B |
| 사료 55% 할인 | 0.45 | 55 | B |
| 건조기 0 하나 빠짐 | 0.10 | 93 | **S** |
| 배송비 포함 시 역전 | 1.09 | 10 | D |

## 튜닝 원칙
- 한 번에 값 하나만 바꾼다
- 근거는 `calibrate.py`의 히스토그램. 감으로 바꾸지 않는다
- 오탐 1건 > 놓친 딜 10건 (정밀도 우선)
