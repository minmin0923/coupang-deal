# 실행 주기 설계 근거

## GitHub Actions 과금 규칙

| 규칙 | 내용 |
|---|---|
| 무료 한도 (Private) | 월 **2,000분** |
| 과금 단위 | **1분 단위 올림** — 40초 실행도 1분으로 청구 |
| Public 레포 | **무제한 무료** |

핵심: 실행 **시간**을 줄이는 것보다 실행 **횟수**를 관리하는 게 전부다.
40초를 30초로 줄여도 청구는 똑같이 1분이다.

## 1회 실행 시간 내역 (추정)

| 단계 | 시간 |
|---|---|
| checkout | 3초 |
| setup-python (pip 캐시 적용) | 5초 |
| pip install | 4초 |
| 쿠팡 API 4~7개 카테고리 | 12~18초 |
| 판정·발송 | 3~8초 |
| git commit/push | 4초 |
| **합계** | **31~42초 → 1분 청구** |

## 주기별 비용

| 주기 | 하루 실행 | 월 사용 | 한도 대비 | 판정 |
|---|---|---|---|---|
| 15분 균등 | 96회 | 2,880분 | 144% | ❌ 초과 |
| 15분(활발)+60분(심야) | 72회 | 2,160분 | 108% | ❌ 초과 |
| **20분(활발)+60분(심야)** | **56회** | **1,680분** | **84%** | ✅ **채택** |
| 30분 균등 | 48회 | 1,440분 | 72% | 여유 있으나 놓침 많음 |

15% 마진을 남긴 이유: 수동 실행, 재실행, 간혹 1분을 넘기는 실행에 대비.

## 채택한 스케줄

```
활발 KST 07:00~22:59  → 20분 간격  (48회)
심야 KST 23:00~06:59  → 60분 간격  ( 8회)
                        하루 56회
```

심야를 느슨하게 잡은 이유: 가격오류가 나도 그 시간엔 구독자가 안 보고,
아침 07:00 데일리 발송에서 어차피 잡힌다.

## 워크플로를 하나로 합친 이유

기존엔 `collect.yml`(데일리 4회) + `hunt.yml`(급락 감시)로 나뉘어 있었다.
합치면 데일리용 4회 실행분(월 120분)이 그대로 절약된다.

`auto` 모드가 현재 시각을 보고 스스로 결정한다.

| 시각 | 동작 |
|---|---|
| KST 07 / 12 / 18 / 22시의 정시~19분 | **데일리 발송** (7개 카테고리 + 골드박스) |
| 그 외 전부 | **급락 감시** (4개 카테고리, 있을 때만 발송) |

## 한도가 부족해지면

우선순위 순으로:

1. **레포를 Public으로 전환** — Actions 무제한. 코드만 공개되고 Secrets는 안전
2. 심야 구간을 120분 간격으로 (`0 14-21` → `0 14,16,18,20`) → 하루 52회
3. `HUNT_CATEGORIES`를 2개로 축소 → 실행 시간 단축(청구는 동일하나 안정성↑)
4. 활발 시간대를 KST 09:00~22:00으로 축소

## 사용량 확인 방법

GitHub → 우측 상단 프로필 → **Settings** → 좌측 **Billing and licenses**
→ **Actions** 항목에서 이번 달 사용 분수를 확인.

매월 1일 초기화된다. 20일쯤에 1,200분을 넘겼다면 주기를 늦출 것.

---

# Public 전환 + 상주 감시 (실시간 모드)

레포를 **Public**으로 바꾸면 Actions가 **무제한 무료**가 된다.
그러면 위의 계산이 전부 무의미해지고, 훨씬 촘촘하게 돌릴 수 있다.

## cron으로는 "즉시"가 안 되는 이유

| 제약 | 내용 |
|---|---|
| 최소 간격 | 5분 |
| 실제 지연 | 부하 시 10~30분 밀림. 아예 건너뛰기도 함 |

그래서 cron 대신 **러너를 켜둔 채 내부에서 루프를 도는** 방식(`watch`)을 쓴다.

## 상주 감시 구조

```
watch.yml → 러너 1개를 290분(약 5시간) 켜둠
            └ 3분마다 순회 → 급락 있으면 즉시 발송
            └ 36분마다 이력 DB를 커밋
cron 0 */5 * * *  → 5시간마다 새 러너가 이어받음 (하루 5교대)
concurrency: cancel-in-progress → 겹치면 새 러너가 옛 러너를 대체
```

| 항목 | 값 |
|---|---|
| 순회 간격 | **3분** (`WATCH_INTERVAL_SEC`) |
| 하루 순회 | 약 **480회** (기존 56회의 8.5배) |
| 쿠팡 API 호출 | 하루 약 1,920콜 |
| Actions 사용 | 하루 1,450분 → **Public이라 무료** |

## 데일리 발송도 이 루프가 처리한다

KST 07/12/18/22시가 되면 그 순회에서 전체 카테고리 + 골드박스를 발송한다.
같은 슬롯을 두 번 발송하지 않도록 `meta` 테이블에 기록한다.

**그래서 `bot.yml`은 삭제해야 한다.** 두 워크플로가 동시에 DB를 쓰면 git 충돌이 난다.

## 순회 간격을 더 줄이려면

`config.py`의 `WATCH_INTERVAL_SEC`만 바꾸면 된다.

| 값 | 간격 | 하루 순회 | 쿠팡 API/일 | 비고 |
|---|---|---|---|---|
| 300 | 5분 | 288회 | 1,152콜 | 안전 |
| **180** | **3분** | **480회** | **1,920콜** | **기본값** |
| 120 | 2분 | 720회 | 2,880콜 | 쿠팡 쪽 제한 주의 |
| 60 | 1분 | 1,440회 | 5,760콜 | 차단 위험 |

쿠팡 파트너스 API의 정확한 호출 한도는 공개돼 있지 않다.
429(Too Many Requests)가 로그에 자주 보이면 값을 올릴 것.
`coupang.py`에 429 재시도 로직이 이미 들어 있어 즉시 실패하지는 않는다.

## Private로 되돌리려면

`watch.yml`을 삭제하고 `bot.yml`을 되살리면 된다. 두 파일 모두 보관해 둘 것.
